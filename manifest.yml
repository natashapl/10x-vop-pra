applications:
- name: pra-app
  buildpacks:
    - nodejs_buildpack
  memory: 256M
  disk_quota: 512M
  instances: 1
  random-route: true
  command: node server.js
  services:
    - pra-navigator-storage
